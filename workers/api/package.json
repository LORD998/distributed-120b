{
  "name": "@distributed-120b/api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — API Gateway, router de inferência, autenticação, cache e streaming",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply distributed_120b --local",
    "db:migrate:remote": "wrangler d1 migrations apply distributed_120b --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.57.1"
  }
}